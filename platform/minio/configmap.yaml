apiVersion: v1
kind: ConfigMap
metadata:
  name: minio-policies
data:
  restricted-policy.json: |
    {
      "Version": "2012-10-17",
      "Statement": [
        {
          "Effect": "Allow",
          "Action": ["s3:*"],
          "Resource": ["arn:aws:s3:::*"],
          "Condition": {
            "StringEquals": {
              "jwt:email": "daniel.boluda.fdez@gmail.com"
            }
          }
        }
      ]
    }
